[workspace]
# Keep the defaults simple and explicit. We rely on Cargo manifests for:
# - which crates are publishable (`publish = false` disables)
# - per-crate versioning (explicit `version = ...` for modules/sdks, and `version.workspace = true` for ModKit libs)
#
# NOTE: release-plz may temporarily checkout commits while computing diffs.
# Some repos can end up "dirty" due to generated/normalized files, so we allow it.
allow_dirty = true

# Add a stable label to the release PR so the publish workflow can filter on it.
pr_labels = ["release-plz"]
changelog_update = true
git_release_enable = true

# We keep a single changelog for the repo. release-plz will update it in the release PR.
changelog_path = "CHANGELOG.md"

# Enable semver checks by default (can be overridden per package if needed later).
semver_check = true

# To prevent crates.io throttling
publish_timeout = "10m"

[changelog]
body = """

## [{{ version }}]\
    {%- if release_link -%}\
        ({{ release_link }})\
    {% endif %} \
    - {{ timestamp | date(format="%Y-%m-%d") }}
{% for group, commits in commits
    | filter(attribute="merge_commit", value=false)
    | group_by(attribute="group")
%}
### {{ group | upper_first }}

    {% for commit in commits %}
        {%- if commit.scope -%}
            - *({{commit.scope}})* {% if commit.breaking %}[**breaking**] {% endif %}\
                {{ commit.message }}{{ self::username(commit=commit) }}\
                {%- if commit.links %} \
                    ({% for link in commit.links %}[{{link.text}}]({{link.href}}) {% endfor -%})\
                {% endif %}
        {% else -%}
            - {% if commit.breaking %}[**breaking**] {% endif %}{{ commit.message }}{{ self::username(commit=commit) }}{{ self::pr(commit=commit) }}
        {% endif -%}
    {% endfor -%}
{% endfor %}
{%- if remote.contributors %}
### Contributors
{% for contributor in remote.contributors %}
    * @{{ contributor.username }}
{%- endfor %}
{% endif -%}
{%- macro username(commit) -%}
    {% if commit.remote.username %} (by @{{ commit.remote.username }}){% endif -%}
{% endmacro -%}
{%- macro pr(commit) -%}
    {% if commit.remote.pr_number %} - #{{ commit.remote.pr_number }}{% endif -%}
{% endmacro -%}
"""

# ModKit Framework - unified release
# Only cf-modkit gets changelog entries and GitHub releases
# All other cf-modkit-* crates are published to crates.io but without separate changelog/releases
[[package]]
name = "cf-modkit"
changelog_update = true
git_release_enable = true

[[package]]
name = "cf-modkit-auth"
changelog_update = false
git_release_enable = false

[[package]]
name = "cf-modkit-db"
changelog_update = false
git_release_enable = false

[[package]]
name = "cf-modkit-db-macros"
changelog_update = false
git_release_enable = false

[[package]]
name = "cf-modkit-errors"
changelog_update = false
git_release_enable = false

[[package]]
name = "cf-modkit-errors-macro"
changelog_update = false
git_release_enable = false

[[package]]
name = "cf-modkit-macros"
changelog_update = false
git_release_enable = false

[[package]]
name = "cf-modkit-macros-tests"
changelog_update = false
git_release_enable = false
publish = false

[[package]]
name = "cf-modkit-node-info"
changelog_update = false
git_release_enable = false

[[package]]
name = "cf-modkit-odata"
changelog_update = false
git_release_enable = false

[[package]]
name = "cf-modkit-odata-macros"
changelog_update = false
git_release_enable = false

[[package]]
name = "cf-modkit-sdk"
changelog_update = false
git_release_enable = false

[[package]]
name = "cf-modkit-security"
changelog_update = false
git_release_enable = false

[[package]]
name = "cf-modkit-transport-grpc"
changelog_update = false
git_release_enable = false

[[package]]
name = "cf-modkit-utils"
changelog_update = false
git_release_enable = false

# System SDK libraries - published to crates.io but without separate changelog/releases
[[package]]
name = "cf-system-sdks"
changelog_update = false
git_release_enable = false

[[package]]
name = "cf-system-sdk-directory"
changelog_update = false
git_release_enable = false

# System modules and their SDKs - published to crates.io but without separate changelog/releases
[[package]]
name = "cf-api-gateway"
changelog_update = false
git_release_enable = false

[[package]]
name = "cf-grpc-hub"
changelog_update = false
git_release_enable = false

[[package]]
name = "cf-file-parser"
changelog_update = false
git_release_enable = false

[[package]]
name = "cf-nodes-registry"
changelog_update = false
git_release_enable = false

[[package]]
name = "cf-nodes-registry-sdk"
changelog_update = false
git_release_enable = false

[[package]]
name = "cf-module-orchestrator"
changelog_update = false
git_release_enable = false

[[package]]
name = "cf-types-registry"
changelog_update = false
git_release_enable = false

[[package]]
name = "cf-types-registry-sdk"
changelog_update = false
git_release_enable = false

[[package]]
name = "cf-simple-user-settings"
publish = false
changelog_update = false
git_release_enable = false

[[package]]
name = "cf-simple-user-settings-sdk"
publish = false
changelog_update = false
git_release_enable = false

[[package]]
name = "cf-tenant-resolver-sdk"
changelog_update = false
git_release_enable = false

[[package]]
name = "cf-tenant-resolver"
changelog_update = false
git_release_enable = false

[[package]]
name = "cf-authn-resolver-sdk"
changelog_update = false
git_release_enable = false

[[package]]
name = "cf-authn-resolver"
changelog_update = false
git_release_enable = false

[[package]]
name = "cf-authz-resolver-sdk"
changelog_update = false
git_release_enable = false

[[package]]
name = "cf-authz-resolver"
changelog_update = false
git_release_enable = false

[[package]]
name = "cf-oagw-sdk"
changelog_update = false
git_release_enable = false

[[package]]
name = "cf-oagw"
changelog_update = false
git_release_enable = false

[[package]]
name = "cf-credstore-sdk"
changelog_update = false
git_release_enable = false

[[package]]
name = "cf-credstore"
changelog_update = false
git_release_enable = false

[[package]]
name = "cf-mini-chat-sdk"
changelog_update = false
git_release_enable = false

[[package]]
name = "cf-mini-chat"
changelog_update = false
git_release_enable = false

[[package]]
name = "cf-static-mini-chat-audit-plugin"
changelog_update = false
git_release_enable = false
