{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "gts://gts.x.llmgw.streaming.event.v1~",
  "title": "StreamingEventBase",
  "description": "Base type for all Open Responses streaming events. All concrete event types inherit from this via GTS type inheritance.",
  "type": "object",
  "properties": {
    "type": {
      "type": "string",
      "description": "Event type — must match the SSE 'event' field."
    },
    "sequence_number": {
      "type": "integer",
      "minimum": 0,
      "description": "Monotonically increasing sequence number for ordering."
    }
  },
  "required": ["type", "sequence_number"],
  "additionalProperties": true
}
