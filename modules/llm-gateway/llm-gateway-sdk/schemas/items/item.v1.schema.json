{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "gts://gts.x.llmgw.items.item.v1~",
  "title": "Item",
  "description": "Base type for all input and output items. Concrete item types inherit from this via GTS type inheritance, discriminated by the 'type' field. Output items produced by a response can be passed back as input items in subsequent requests.",
  "type": "object",
  "properties": {
    "type": {
      "type": "string",
      "description": "Item type discriminator."
    },
    "id": {
      "type": ["string", "null"],
      "description": "Item identifier. Optional for input items, required for output items."
    },
    "status": {
      "type": ["string", "null"],
      "enum": ["in_progress", "completed", "incomplete", null],
      "description": "Item lifecycle status. Optional for input items, required for most output items."
    }
  },
  "required": ["type"],
  "additionalProperties": true
}
