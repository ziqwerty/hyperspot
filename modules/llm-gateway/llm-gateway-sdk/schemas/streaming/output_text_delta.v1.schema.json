{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "gts://gts.x.llmgw.streaming.event.v1~x.llmgw.streaming.output_text_delta.v1~",
  "title": "Response output text delta event",
  "description": "A streaming event indicating output text was incrementally added.",
  "allOf": [
    { "$ref": "gts://gts.x.llmgw.streaming.event.v1~" },
    {
      "properties": {
        "type": {
          "type": "string",
          "const": "response.output_text.delta"
        },
        "item_id": {
          "type": "string",
          "description": "The ID of the item that was updated."
        },
        "output_index": {
          "type": "integer",
          "minimum": 0,
          "description": "The index of the output item that was updated."
        },
        "content_index": {
          "type": "integer",
          "minimum": 0,
          "description": "The index of the content part that was updated."
        },
        "delta": {
          "type": "string",
          "description": "The text delta that was appended."
        },
        "logprobs": {
          "type": "array",
          "items": {
            "type": "object",
            "properties": {
              "token": { "type": "string" },
              "logprob": { "type": "number" },
              "bytes": { "type": "array", "items": { "type": "integer" } },
              "top_logprobs": {
                "type": "array",
                "items": {
                  "type": "object",
                  "properties": {
                    "token": { "type": "string" },
                    "logprob": { "type": "number" },
                    "bytes": { "type": "array", "items": { "type": "integer" } }
                  },
                  "required": ["token", "logprob"]
                }
              }
            },
            "required": ["token", "logprob", "top_logprobs"]
          },
          "description": "The token log probabilities emitted with the delta, if any."
        }
      },
      "required": ["type", "sequence_number", "item_id", "output_index", "content_index", "delta"]
    }
  ]
}
